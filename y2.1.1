#include <stdio.h>
struct d_type
{
	int year;
	int month;
	int day;
}
struct st_type
{
	char num[7];
	char name[21];
    char sex;
	struct d_type birthday;
	float score;
	char add[31];
}
int main(void)
{	
	int n,i;
	printf("请输入学生数量");
	scanf("%d",n);
	for(i=1;i<=n;i++)
	{
		printf("请输入学生学号");
		scanf("%c",stu[i].num);
		printf("请输入学生姓名");
		scanf("%c.stu[i].name");
		printf("请输入学生性别");
		scanf("%c",stu[i].sex);
		printf("请输入学生生日");
		scanf("%d",stu[i].birthday);
		printf("请输入学生成绩");
		scanf("");


	}
